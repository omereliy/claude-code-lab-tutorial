# --- Claude Code: optional shell config -------------------------------------
# Append to ~/.zshrc (or ~/.bashrc). Open a fresh shell after editing.

# The native installer puts the binary here. Uncomment if `claude` isn't found.
# export PATH="$HOME/.local/bin:$PATH"

# Optional shell-specific model default. PREFER ~/.claude/settings.json instead;
# only use this if you want a per-shell override. The env var WINS over settings.
# export ANTHROPIC_MODEL=opus

# Optional per-shell effort override. Persistent default belongs in settings.json.
# export CLAUDE_CODE_EFFORT_LEVEL=high

# Behind the campus/corporate proxy and auth hangs? Set your proxy:
# export HTTPS_PROXY=http://proxy.example.bgu.ac.il:8080
# ----------------------------------------------------------------------------
