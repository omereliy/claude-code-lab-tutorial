<!--
BYOR CLAUDE.md skeleton. Copy to your repo root as CLAUDE.md and fill in.
Delete every bracketed prompt as you replace it. Keep it SHORT (50-150 lines).
Write what is TRUE NOW, not what should be true (Module 2). Delete sections that
don't apply rather than leaving them empty.
-->

# [project name]

[One sentence: what this repo is. e.g. "DQN baselines for the gridworld MARL
benchmark; research code, not a library."]

## Run and test

- Install: [exact command, e.g. `pip install -e .` or `conda env create -f env.yml`]
- Tests: [exact command, e.g. `pytest tests/`]
- Lint/format: [exact command, or delete this line if you don't]
- Run an experiment: [exact command, if applicable]

## Code conventions

- Naming: [the real convention. If it's mixed, say which is canonical and which
  is deprecated — Claude can't infer this.]
- Types: [required? optional? on public functions only?]
- [Any non-default line length, docstring style, etc. Delete if default.]

## Where things live

- Entry point(s): [file(s)]
- Core logic: [where the real algorithm/model lives]
- New [utilities / metrics / envs] go in: [path] — not [the wrong place people
  reach for first]
- Tests: [path], one file per [module / component]

## Do not touch

- [Files that are load-bearing in non-obvious ways, or dead/experimental code
  Claude should not read or mimic. e.g. "legacy_train.py — used by old configs,
  do not refactor." Delete this section only if genuinely nothing qualifies.]

## What I'm working on

[The current direction, in a sentence or two. Update when it changes. This is
the section that goes stale fastest — treat it like code.]

## Don't

- [Concrete don'ts. e.g. "Don't add dependencies without asking. Don't reformat
  unrelated code. Don't change the experiment config defaults."]
